[package]
name = "rover-mechanic"
version = "0.1.0"
edition = "2021"

[dependencies]
include_dir = "0.7.4"
jni = "0.21.1"
mlua = { version = "0.9.9", features = ["lua54", "vendored"] }

[lib]
crate-type = ["staticlib", "cdylib"]

