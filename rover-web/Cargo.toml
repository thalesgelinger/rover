[package]
name = "rover-web"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0.100"
bytes = "1.10"
mlua = { version = "0.11.5", features = ["anyhow", "vendored", "luajit52"] }
rover_server = { path = "../rover-server" }
