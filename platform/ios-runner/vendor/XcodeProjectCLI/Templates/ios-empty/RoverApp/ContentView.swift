import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("Rover placeholder")
                .font(.title)
            Text("Lua/Skia pipeline pending")
                .font(.subheadline)
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
