[package]
name = "rover-web-wasm"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
mlua = { version = "0.11.5", default-features = false, features = ["anyhow", "lua54", "vendored"] }
